using System;
using System.Numerics;
using System.Collections.Generic;

namespace _2021_04_22_Raylib_ImGUI_Tiled_Study
{
    public static class SKILL_EVENTS
    {
        private static Dictionary<>
        public void LOAD_SKILLS(List<EVENT_HANDLER> list_of_handlers, List<EventArgs> arguments)
        {
            List<EVENT_HANDLER> LIST_OF_HANDLERS = list_of_handlers;
            
            foreach (var item in LIST_OF_HANDLERS)
            {
                
            }

            new EVENT_MANAGER(LIST_OF_HANDLERS, LIST_OF_HANDLERS.Count);
        }
    }
}